# Notes

- Keep your dataset organized by class folders (one folder per category).
- Use image resizing + normalization consistently.
- If accuracy is low:
  - increase training data
  - apply augmentation
  - use transfer learning (e.g., MobileNetV2, EfficientNet)
